<!DOCTYPE html>
<html>
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <style>
      /* Email styles need to be inline */
    </style>
  </head>

  <body>
  Twoje podanie o nazwie <%= @notification.recrutation.title %> zmieniło status na  <%= @notification.status == 1 ? 'zaakceptowany.' : 'odrzucony.' %>
  Życzymy miłego dnia

    <%= yield %>
  </body>
</html>
